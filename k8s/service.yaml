apiVersion: v1
kind: Service

metadata:
  name: secure-banking-service

spec:
  selector:
    app: secure-banking-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: NodePort
